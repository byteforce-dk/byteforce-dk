name: deploy-to-production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: copy-to-cloudstorage
        uses: google-github-actions/upload-cloud-storage@v1
        with:
          